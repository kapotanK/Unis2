package com.kapot.unis2.ui.util;

import javafx.scene.Node;
import javafx.scene.control.Tooltip;

public class FxUtil {

//    public static void disableTooltipAutoHide(Tooltip tooltip) { //TODO: disable tooltip autohide
//        tooltip.showingProperty().addListener((obs) -> {
//            System.out.println("showlist " + tooltip.isShowing() + " " + tooltip.isActivated());
//            tooltip.setOnHiding(event -> {
//                System.out.println(tooltip.getOwnerNode());
//            });
//            //tooltip.show(tooltip.getOwnerWindow(), tooltip.getAnchorX(), tooltip.getAnchorY());
//        });
//    }

}
